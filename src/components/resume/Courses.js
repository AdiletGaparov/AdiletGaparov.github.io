import React from 'react';
import Container from "@material-ui/core/Container";

const Courses = (props) => {
    return (
        <Container>
            Table with Courses
        </Container>
    )
}

export default Courses;